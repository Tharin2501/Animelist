import styled from "styled-components";
import {{pascalCase name}}Type from './types/{{pascalCase name}}Type';

const Wrapper = styled.div`

`

const {{pascalCase name}}: React.FunctionComponent<{{pascalCase name}}Type> = ({children}) => {
  return (
    <Wrapper>
     {children}
    </Wrapper>
  );
};

export default {{pascalCase name}};